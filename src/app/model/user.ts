export class User {
  constructor(public id_user?: number,
              public user_name?: string,
              public email?: string,
              public password?: string,
              public phone?: number,
  ) {
  }
}
